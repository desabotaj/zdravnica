# 🐧 Деплой на локальный Linux сервер

## 🌐 Нужен ли домен?

**НЕТ, домен не обязателен!** Можно использовать:
- ✅ **IP-адрес сервера** (например: `http://192.168.1.100:8001`)
- ✅ **Локальный домен** (например: `http://techrepair.local`)
- ✅ **Внешний IP** (если сервер доступен из интернета: `http://ваш-внешний-ip:8001`)
- ✅ **Купить домен** (если нужен красивый адрес типа `techrepair.ru`)

---

## 🚀 Вариант 1: Без домена (по IP-адресу)

### Шаг 1: Узнайте IP вашего сервера

```bash
# Внутренний IP (для локальной сети)
ip addr show | grep "inet " | grep -v 127.0.0.1

# Или проще:
hostname -I

# Внешний IP (если сервер доступен из интернета)
curl ifconfig.me
```

### Шаг 2: Настройте файрвол

```bash
# Откройте порт 8001
sudo ufw allow 8001/tcp

# Или для iptables:
sudo iptables -A INPUT -p tcp --dport 8001 -j ACCEPT
```

### Шаг 3: Запустите сервер

```bash
cd /path/to/avito
python3 production_server.py
```

### Шаг 4: Доступ к сайту

- **Локально**: `http://localhost:8001` или `http://127.0.0.1:8001`
- **Из сети**: `http://192.168.1.100:8001` (замените на ваш IP)
- **Из интернета**: `http://ваш-внешний-ip:8001`

---

## 🌐 Вариант 2: С доменом (если хотите)

### Где купить .ru домен?

**Рекомендуемые регистраторы:**
1. **REG.RU** - от 199₽/год - [reg.ru](https://www.reg.ru)
2. **Timeweb** - от 179₽/год - [timeweb.com](https://timeweb.com)
3. **2domains** - от 149₽/год - [2domains.ru](https://2domains.ru)
4. **Nic.ru** - от 199₽/год - [nic.ru](https://nic.ru)

**Популярные домены:**
- `.ru` - от 149₽/год
- `.рф` - от 199₽/год
- `.com` - от 599₽/год

### Настройка DNS для домена

После покупки домена нужно настроить DNS записи:

1. **Вариант A: A-запись (если у вас статический IP)**
   ```
   Тип: A
   Имя: @ (или оставьте пустым)
   Значение: ваш-внешний-ip-адрес
   TTL: 3600
   ```

2. **Вариант B: Поддомен**
   ```
   Тип: A
   Имя: techrepair (или любое другое)
   Значение: ваш-внешний-ip-адрес
   TTL: 3600
   ```
   Тогда сайт будет доступен по: `http://techrepair.ваш-домен.ru`

3. **Вариант C: Динамический DNS (если IP меняется)**
   - Используйте сервисы типа `noip.com`, `duckdns.org` (бесплатно)
   - Или настройте автоматическое обновление IP через скрипт

---

## 📦 Полная инструкция по настройке на Linux

### 1. Подготовка сервера

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Python 3.11+
sudo apt install python3 python3-pip -y

# Проверьте версию
python3 --version  # Должно быть 3.10+
```

### 2. Загрузка файлов проекта

```bash
# Создайте папку для проекта
sudo mkdir -p /var/www/techrepair
sudo chown $USER:$USER /var/www/techrepair

# Загрузите файлы (через SCP, Git, или вручную)
# Например через SCP с вашего компьютера:
# scp -r deploy_package/* user@ваш-сервер:/var/www/techrepair/

# Или через Git:
cd /var/www/techrepair
git clone <ваш-репозиторий> .
```

### 3. Настройка systemd сервиса

Создайте файл `/etc/systemd/system/techrepair.service`:

```bash
sudo nano /etc/systemd/system/techrepair.service
```

Вставьте:

```ini
[Unit]
Description=TechRepair CRM Server
After=network.target

[Service]
Type=simple
User=www-data
Group=www-data
WorkingDirectory=/var/www/techrepair
Environment="PORT=8001"
Environment="HOST=0.0.0.0"
ExecStart=/usr/bin/python3 /var/www/techrepair/production_server.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Сохраните и запустите:

```bash
# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable techrepair

# Запустите сервис
sudo systemctl start techrepair

# Проверьте статус
sudo systemctl status techrepair

# Просмотр логов
sudo journalctl -u techrepair -f
```

### 4. Настройка Nginx (рекомендуется)

```bash
# Установите Nginx
sudo apt install nginx -y
```

Создайте конфиг `/etc/nginx/sites-available/techrepair`:

```bash
sudo nano /etc/nginx/sites-available/techrepair
```

Вставьте (для работы по IP):

```nginx
server {
    listen 80;
    server_name _;  # Принимает любой домен/IP

    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Или для домена:

```nginx
server {
    listen 80;
    server_name techrepair.ru www.techrepair.ru;

    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфиг:

```bash
# Создайте симлинк
sudo ln -s /etc/nginx/sites-available/techrepair /etc/nginx/sites-enabled/

# Удалите дефолтный конфиг (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### 5. Настройка SSL (HTTPS) - опционально

**С Let's Encrypt (бесплатно):**

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получите сертификат (для домена)
sudo certbot --nginx -d techrepair.ru -d www.techrepair.ru

# Автообновление
sudo certbot renew --dry-run
```

**Или самоподписанный сертификат (для тестирования):**

```bash
sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
  -keyout /etc/ssl/private/techrepair.key \
  -out /etc/ssl/certs/techrepair.crt
```

### 6. Настройка файрвола

```bash
# Установите UFW (если не установлен)
sudo apt install ufw -y

# Разрешите SSH (важно!)
sudo ufw allow 22/tcp

# Разрешите HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Или если без Nginx, только порт приложения:
sudo ufw allow 8001/tcp

# Включите файрвол
sudo ufw enable

# Проверьте статус
sudo ufw status
```

---

## 🔧 Настройка для работы из интернета

### Если у вас динамический IP

1. **Используйте динамический DNS:**
   - Зарегистрируйтесь на [noip.com](https://www.noip.com) (бесплатно)
   - Установите клиент на сервер
   - Получите адрес типа `techrepair.ddns.net`

2. **Или настройте автоматическое обновление IP:**
   ```bash
   # Создайте скрипт для обновления DNS
   nano ~/update_dns.sh
   ```

### Если у вас роутер

Настройте проброс портов (Port Forwarding):
- Внешний порт: 80 (HTTP) или 443 (HTTPS)
- Внутренний IP: IP вашего сервера
- Внутренний порт: 8001

---

## ✅ Проверка работы

```bash
# Проверьте что сервер запущен
sudo systemctl status techrepair

# Проверьте что порт слушается
sudo netstat -tulpn | grep 8001

# Проверьте доступность
curl http://localhost:8001/api/stats

# Проверьте логи
sudo journalctl -u techrepair -n 50
```

---

## 🎯 Быстрый старт (минимальная настройка)

Если нужно просто запустить без лишних настроек:

```bash
# 1. Загрузите файлы на сервер
cd /var/www/techrepair

# 2. Запустите напрямую
python3 production_server.py

# 3. Откройте в браузере
# http://ваш-ip:8001
```

**Для постоянной работы используйте systemd (см. выше).**

---

## 📊 Мониторинг

```bash
# Просмотр логов в реальном времени
sudo journalctl -u techrepair -f

# Статистика использования ресурсов
htop
# или
top

# Проверка сетевых подключений
sudo netstat -tulpn | grep python
```

---

## 🆘 Решение проблем

### Проблема: Не могу подключиться извне
- ✅ Проверьте файрвол: `sudo ufw status`
- ✅ Проверьте проброс портов на роутере
- ✅ Убедитесь что сервер слушает `0.0.0.0`, а не `127.0.0.1`

### Проблема: Порт занят
```bash
# Найдите процесс
sudo lsof -i :8001
# Убейте процесс
sudo kill -9 <PID>
```

### Проблема: Права доступа
```bash
# Дайте права на запись
sudo chown -R www-data:www-data /var/www/techrepair
sudo chmod -R 755 /var/www/techrepair
```

---

## 💡 Рекомендации

1. **Используйте Nginx** - он надёжнее чем прямой доступ к Python серверу
2. **Настройте HTTPS** - обязательно для продакшена
3. **Используйте systemd** - для автозапуска и перезапуска при сбоях
4. **Настройте бэкапы** - регулярно сохраняйте `*_data.json` файлы
5. **Мониторинг** - следите за логами и ресурсами

---

**Удачи с настройкой!** 🚀

