# 🔧 Устранение проблем TechRepair CRM

## 🚨 Частые проблемы и решения

### 1. Админ-панель не показывает новые заявки

**Проблема:** Заявки создаются, но не отображаются в админ-панели.

**Решения:**
1. **Обновите страницу** - нажмите F5 или кнопку "🔄 Обновить"
2. **Проверьте консоль браузера** - нажмите F12 → Console
3. **Очистите кэш браузера** - Ctrl+Shift+R
4. **Перезапустите сервер** - закройте и запустите start.bat заново

### 2. Зависание при обновлении статуса

**Проблема:** Кнопка "Сохранить" зависает при изменении статуса.

**Решения:**
1. **Подождите 10 секунд** - есть таймаут запроса
2. **Обновите страницу** - изменения сохранятся локально
3. **Проверьте сервер** - убедитесь что сервер запущен
4. **Перезапустите сервер** если проблема повторяется

### 3. Ошибка "Сервер недоступен"

**Проблема:** Не удается подключиться к серверу.

**Решения:**
1. **Запустите сервер** - двойной клик на start.bat
2. **Проверьте порт** - убедитесь что порт 8001 свободен
3. **Проверьте антивирус** - добавьте папку в исключения
4. **Используйте другой порт** - измените PORT в production_server.py

### 4. Данные не сохраняются

**Проблема:** Заявки исчезают после перезапуска.

**Решения:**
1. **Проверьте файл repairs_data.json** - должен создаваться автоматически
2. **Права доступа** - убедитесь что Python может писать файлы
3. **Место на диске** - проверьте свободное место
4. **Антивирус** - может блокировать создание файлов

### 5. Медленная работа

**Проблема:** Сайт загружается медленно.

**Решения:**
1. **Закройте другие программы** - освободите память
2. **Перезапустите браузер** - очистите память браузера
3. **Используйте другой браузер** - Chrome, Firefox, Edge
4. **Перезагрузите компьютер** если проблема серьезная

## 🧪 Диагностика

### Быстрый тест API
```bash
python quick_test.py
```

### Проверка файла данных
```bash
type repairs_data.json
```

### Проверка порта
```bash
netstat -an | findstr :8001
```

### Остановка всех Python процессов
```bash
taskkill /f /im python.exe
```

## 📞 Логи и отладка

### Включение подробных логов
1. Откройте production_server.py
2. Найдите строку `def log_message`
3. Уберите `pass` и добавьте `print(format % args)`

### Проверка консоли браузера
1. Нажмите F12
2. Перейдите на вкладку Console
3. Ищите ошибки (красные сообщения)
4. Скопируйте текст ошибки для анализа

### Проверка сетевых запросов
1. F12 → Network
2. Обновите страницу
3. Ищите красные запросы (ошибки 404, 500)
4. Кликните на запрос для деталей

## 🔄 Полный сброс

Если ничего не помогает:

1. **Остановите сервер** - закройте все окна Python
2. **Удалите файл данных** - `del repairs_data.json`
3. **Очистите кэш браузера** - Ctrl+Shift+Delete
4. **Перезапустите систему** - `start.bat`
5. **Создайте тестовую заявку** - проверьте работу

## 📱 Контакты поддержки

При серьезных проблемах:
- Сохраните логи сервера
- Сделайте скриншот ошибки
- Опишите последовательность действий
- Укажите версию браузера и ОС

---

**💡 Большинство проблем решается перезапуском сервера и обновлением страницы!**