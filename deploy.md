# 🚀 Деплой TechRepair CRM

## 📋 Готовые файлы для деплоя

- `production_server.py` - основной сервер
- `requirements.txt` - зависимости
- `Procfile` - конфигурация для Heroku/Railway
- `runtime.txt` - версия Python
- `repair_landing.html` - форма заявок
- `repair_admin.html` - админ-панель
- `repairs_data.json` - файл данных (создается автоматически)

## 🌐 Платформы для деплоя

### 1. Heroku (бесплатно)
```bash
# Установите Heroku CLI
heroku create techrepair-crm
git add .
git commit -m "Deploy TechRepair CRM"
git push heroku main
```

### 2. Railway (бесплатно)
```bash
# Подключите GitHub репозиторий к Railway
# Railway автоматически деплоит из main ветки
```

### 3. Render (бесплатно)
```bash
# Создайте Web Service на render.com
# Укажите команду запуска: python production_server.py
```

### 4. PythonAnywhere (бесплатно)
```bash
# Загрузите файлы через веб-интерфейс
# Настройте WSGI приложение
```

### 5. VPS/Dedicated сервер
```bash
# Установите Python 3.11+
sudo apt update
sudo apt install python3 python3-pip

# Загрузите файлы проекта
git clone <your-repo>
cd techrepair-crm

# Запустите сервер
python3 production_server.py

# Для продакшена используйте systemd или supervisor
```

## ⚙️ Переменные окружения

- `PORT` - порт сервера (по умолчанию 8001)
- `HOST` - хост сервера (по умолчанию 0.0.0.0)

## 🔧 Настройка для продакшена

1. **Nginx (рекомендуется)**
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

2. **SSL сертификат**
```bash
# Используйте Let's Encrypt
sudo certbot --nginx -d yourdomain.com
```

3. **Systemd сервис**
```ini
[Unit]
Description=TechRepair CRM
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/techrepair-crm
ExecStart=/usr/bin/python3 production_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

## 📊 Мониторинг

Сервер логирует все операции:
- ✅ Новые заявки
- 🔄 Изменения статусов
- 🗑️ Удаления
- 📊 API запросы

## 🔒 Безопасность

Для продакшена рекомендуется:
- Добавить аутентификацию для админ-панели
- Настроить HTTPS
- Ограничить CORS для конкретных доменов
- Добавить rate limiting

## 📱 Мобильная версия

Все страницы адаптивны и работают на мобильных устройствах.

## 🆘 Поддержка

При проблемах с деплоем:
1. Проверьте логи сервера
2. Убедитесь что порт не занят
3. Проверьте права доступа к файлам
4. Убедитесь что Python 3.6+ установлен