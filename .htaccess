# Apache конфигурация для TechRepair CRM
# Используйте если хостинг работает на Apache

# Перенаправление на Python приложение
RewriteEngine On

# Если файл существует, отдаём его напрямую
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Если папка существует, отдаём её
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Все остальные запросы перенаправляем на production_server.py
# (это нужно только если используете mod_wsgi или CGI)
# Для обычного хостинга с Python приложениями используйте настройки панели

# Кэширование статических файлов
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
</IfModule>

# Сжатие файлов
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json
</IfModule>

# Безопасность заголовков
<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-XSS-Protection "1; mode=block"
</IfModule>

