# 🚀 Деплой TechRepair CRM на российский хостинг

## 📋 Рекомендуемые хостинги с .ru доменами

### 1. **Timeweb** (рекомендуется) ⭐
- ✅ Поддержка Python 3.x
- ✅ Бесплатный SSL
- ✅ Простая панель управления
- ✅ От ~200₽/месяц
- 🌐 [timeweb.com](https://timeweb.com)

### 2. **Beget**
- ✅ Хорошая поддержка Python
- ✅ Бесплатный SSL
- ✅ От ~150₽/месяц
- 🌐 [beget.com](https://beget.com)

### 3. **REG.RU** (VPS)
- ✅ Полный контроль
- ✅ От ~300₽/месяц
- 🌐 [reg.ru](https://www.reg.ru)

### 4. **Selectel** (VPS)
- ✅ Надёжный VPS
- ✅ От ~400₽/месяц
- 🌐 [selectel.ru](https://selectel.ru)

---

## 📦 Подготовка проекта к деплою

### Шаг 1: Упакуйте файлы для загрузки

**Обязательные файлы:**
```
avito/
├── production_server.py    # Основной сервер
├── index.html             # Лендинг
├── admin.html            # Админ-панель
├── styles/               # CSS файлы
│   ├── design-system.css
│   ├── landing.css
│   └── admin.css
├── requirements.txt      # Зависимости (пустой, всё встроено)
└── runtime.txt          # Версия Python
```

**Файлы данных (создадутся автоматически):**
- `repairs_data.json`
- `customers_data.json`
- `inventory_data.json`
- `appointments_data.json`
- `settings_data.json`

---

## 🌐 Деплой на Timeweb (пошагово)

### 1. Регистрация и покупка хостинга
1. Зайдите на [timeweb.com](https://timeweb.com)
2. Выберите тариф "Хостинг" (от 200₽/мес)
3. Зарегистрируйте или подключите домен `.ru`

### 2. Настройка Python приложения

**В панели Timeweb:**
1. Зайдите в раздел **"Python"** или **"Приложения"**
2. Создайте новое Python приложение:
   - **Версия Python**: 3.11 (или 3.10)
   - **Точка входа**: `production_server.py`
   - **Порт**: оставьте по умолчанию (или укажите через переменную окружения)

### 3. Загрузка файлов

**Через FTP/SFTP:**
```bash
# Используйте FileZilla или WinSCP
# Подключитесь к серверу Timeweb
# Загрузите все файлы в корневую папку домена
```

**Или через панель управления:**
1. Зайдите в **"Файловый менеджер"**
2. Загрузите файлы через веб-интерфейс

### 4. Настройка переменных окружения

В панели Timeweb найдите раздел **"Переменные окружения"** и добавьте:
```
PORT=8001
HOST=0.0.0.0
```

### 5. Запуск приложения

В панели Timeweb:
1. Найдите ваше Python приложение
2. Нажмите **"Запустить"** или **"Перезапустить"**
3. Проверьте логи на наличие ошибок

### 6. Настройка домена

1. В панели Timeweb зайдите в **"Домены"**
2. Подключите ваш `.ru` домен к приложению
3. Дождитесь распространения DNS (обычно 5-30 минут)

---

## 🌐 Деплой на Beget (альтернатива)

### 1. Регистрация
1. Зайдите на [beget.com](https://beget.com)
2. Выберите тариф с поддержкой Python

### 2. Настройка Python
1. В панели Beget зайдите в **"Python"**
2. Создайте приложение:
   - **Скрипт**: `production_server.py`
   - **Версия**: Python 3.11

### 3. Загрузка файлов
Используйте **"Файловый менеджер"** или **FTP**

### 4. Запуск
В панели нажмите **"Запустить приложение"**

---

## 🖥️ Деплой на VPS (REG.RU / Selectel)

### 1. Подключение к серверу
```bash
ssh root@ваш-сервер.ru
```

### 2. Установка Python
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip -y

# Проверка версии
python3 --version  # Должно быть 3.10+
```

### 3. Загрузка файлов
```bash
# Вариант 1: через Git
git clone <ваш-репозиторий>
cd avito

# Вариант 2: через SCP
scp -r ./avito/* root@ваш-сервер.ru:/var/www/techrepair/
```

### 4. Установка зависимостей
```bash
cd /var/www/techrepair
# Зависимостей нет, всё встроено в Python
```

### 5. Настройка systemd сервиса

Создайте файл `/etc/systemd/system/techrepair.service`:
```ini
[Unit]
Description=TechRepair CRM Server
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/techrepair
Environment="PORT=8001"
Environment="HOST=0.0.0.0"
ExecStart=/usr/bin/python3 /var/www/techrepair/production_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:
```bash
sudo systemctl daemon-reload
sudo systemctl enable techrepair
sudo systemctl start techrepair
sudo systemctl status techrepair
```

### 6. Настройка Nginx

Установите Nginx:
```bash
sudo apt install nginx -y
```

Создайте конфиг `/etc/nginx/sites-available/techrepair`:
```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    location / {
        proxy_pass http://127.0.0.1:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

Активируйте конфиг:
```bash
sudo ln -s /etc/nginx/sites-available/techrepair /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 7. Настройка SSL (Let's Encrypt)
```bash
sudo apt install certbot python3-certbot-nginx -y
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
```

---

## ✅ Проверка после деплоя

1. **Откройте сайт**: `http://ваш-домен.ru`
2. **Проверьте API**: `http://ваш-домен.ru/api/stats`
3. **Проверьте админку**: `http://ваш-домен.ru/admin.html`
4. **Проверьте создание заявки**: отправьте тестовую заявку через форму

---

## 🔒 Безопасность

### Рекомендации:
1. ✅ **HTTPS обязателен** - используйте бесплатный SSL (Let's Encrypt)
2. ✅ **Логин/пароль** уже есть в коде (toseh / 12345)
3. ⚠️ **Смените пароль** в `index.html` перед деплоем!
4. ✅ **Ограничьте доступ** к админке по IP (опционально, через Nginx)

### Смена пароля:
Откройте `index.html` и найдите:
```javascript
const STAFF_LOGIN = 'toseh';
const STAFF_PASSWORD = '12345';
```
Замените на свои значения.

---

## 📊 Мониторинг и логи

### Просмотр логов (VPS):
```bash
# Логи systemd
sudo journalctl -u techrepair -f

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### Просмотр логов (Timeweb/Beget):
- Используйте панель управления хостинга
- Раздел **"Логи"** или **"Мониторинг"**

---

## 🆘 Решение проблем

### Проблема: Приложение не запускается
- ✅ Проверьте версию Python (`python3 --version`)
- ✅ Проверьте права на файлы (`chmod +x production_server.py`)
- ✅ Проверьте логи ошибок

### Проблема: Порт занят
- ✅ Измените `PORT` в переменных окружения
- ✅ Проверьте что порт не используется: `netstat -tulpn | grep 8001`

### Проблема: Файлы данных не сохраняются
- ✅ Проверьте права на запись в папку проекта
- ✅ Убедитесь что файлы `.json` создаются

### Проблема: Статические файлы не загружаются
- ✅ Проверьте пути к CSS файлам в HTML
- ✅ Убедитесь что папка `styles/` загружена на сервер

---

## 📞 Поддержка хостингов

- **Timeweb**: [help.timeweb.com](https://help.timeweb.com) | +7 (800) 100-95-24
- **Beget**: [beget.com/ru/support](https://beget.com/ru/support) | +7 (495) 649-16-26
- **REG.RU**: [www.reg.ru/support](https://www.reg.ru/support) | +7 (495) 363-36-36

---

## 🎯 Быстрый старт (Timeweb)

1. Купите хостинг на Timeweb
2. Зарегистрируйте `.ru` домен
3. В панели создайте Python приложение
4. Загрузите файлы через FTP
5. Запустите приложение
6. Готово! 🎉

---

**Удачи с деплоем!** 🚀

